plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.google.services)
}

// Load local.properties safely to read production configuration variables
Properties properties = new Properties()
if (project.rootProject.file('local.properties').exists()) {
    properties.load(project.rootProject.file('local.properties').newDataInputStream())
}

def fallbackBaseUrl = '"https://android-9t8m.onrender.com"'
def prodBaseUrl = properties.getProperty("production.base.url") ? '"' + properties.getProperty("production.base.url") + '"' : fallbackBaseUrl
def admobAppId = properties.getProperty("admob.app.id", "ca-app-pub-6992986179206821~1164725629")
def applovinSdkKey = properties.getProperty("applovin.sdk.key", "YOUR_APPLOVIN_SDK_KEY_HERE")

android {
    namespace 'com.mohit.camverz'
    compileSdk = 36

    defaultConfig {
        applicationId = "com.mohit.camverz"
        minSdk = 24
        targetSdk = 36
        versionCode = 1
        versionName = "1.0.0"

        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
        
        ndk {
            abiFilters 'arm64-v8a', 'x86_64'
        }

        // Define BASE_URL for all API calls
        buildConfigField "String", "BASE_URL", prodBaseUrl

        // Inject variables into AndroidManifest.xml dynamically
        manifestPlaceholders = [
            admobAppId: admobAppId,
            applovinSdkKey: applovinSdkKey
        ]
    }

    buildFeatures {
        buildConfig = true
    }

    signingConfigs {
        release {
            storeFile file("camverz-mohit-release.jks")
            storePassword "Mohit__8120__"
            keyAlias "key0"
            keyPassword "Mohit__8120__"
        }
        debug {
            storeFile file(System.getProperty("user.home") + "/.android/debug.keystore")
            storePassword "Mohit__8120__"
            keyAlias "androiddebugkey"
            keyPassword "Mohit__8120__"
        }
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.debug
        }
        release {
            signingConfig signingConfigs.release
            minifyEnabled = true
            shrinkResources = true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            ndk {
                debugSymbolLevel 'SYMBOL_TABLE'
            }
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_11
        targetCompatibility = JavaVersion.VERSION_11
    }

    kotlinOptions {
        jvmTarget = '11'
    }

    packaging {
        resources {
            excludes += 'META-INF/native-image/org.bytedeco.javacpp/jni-config.json'
        }
        jniLibs {
            useLegacyPackaging = false
        }
    }
}

dependencies {
    implementation libs.androidx.core.ktx
    implementation libs.androidx.appcompat
    implementation libs.material

    // Google Sign-In & Location
    implementation libs.play.services.auth
    implementation libs.play.services.location
    implementation libs.play.app.update

    // Retrofit & OkHttp (Networking)
    implementation libs.retrofit
    implementation libs.retrofit.converter.gson
    implementation libs.okhttp.logging.interceptor
    implementation libs.okhttp

    // Encrypted SharedPreferences (secure JWT storage) & LocalBroadcastManager
    implementation libs.security.crypto
    implementation libs.localbroadcastmanager

    // Socket.IO Client for real-time signaling
    implementation (libs.socket.io.client) {
        exclude group: 'org.json', module: 'json'
    }

    // JSON & WebRTC SDK
    implementation libs.json
    implementation libs.webrtc.sdk

    // UI Components
    implementation libs.circleimageview
    implementation libs.lottie
    implementation libs.shimmer
    implementation libs.cardview
    implementation libs.constraintlayout
    implementation libs.swiperefreshlayout
    implementation libs.paging.runtime

    // Glide & Coil (Image Loading)
    implementation libs.glide
    annotationProcessor libs.glide.compiler
    implementation libs.coil

    // CameraX (16 KB page size aligned, upgraded to 1.6.2)
    implementation libs.camera.core
    implementation libs.camera.camera2
    implementation libs.camera.lifecycle
    implementation libs.camera.view

    // Guava for ListenableFuture
    implementation libs.guava

    // For face detection - using Google Play Services ML Kit (16 KB page size compliant & unbundled)
    implementation libs.play.services.mlkit.face.detection

    // Google Mobile Ads (AdMob) & LevelPlay SDK
    implementation libs.play.services.ads
    implementation libs.facebook.audience.network
    implementation libs.unity.ads
    implementation libs.ironsource.sdk

    // LevelPlay adapters for bidding networks
    implementation libs.ironsource.adapter.admob
    implementation libs.ironsource.adapter.facebook
    implementation libs.ironsource.adapter.unity
    implementation libs.media3.exoplayer
    implementation libs.androidx.browser

    // Google Play Install Referrer (for direct Play Store affiliate tracking)
    implementation libs.install.referrer

    testImplementation libs.junit
    androidTestImplementation libs.androidx.junit
    androidTestImplementation libs.androidx.espresso.core
}
